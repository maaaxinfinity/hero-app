.gradle/
build/
*.iml
.idea/
.kotlin/
local.properties
captures/

# signing secrets — never commit
composeApp/keystore.jks
keystore.properties

# Firebase FCM config — never commit (public repo); provided locally / in CI
google-services.json
**/google-services.json
